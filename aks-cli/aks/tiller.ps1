SubMenu @{
   "check" = "Check Helm server side component (Tiller)"
   "install" = "Install Helm server side component (Tiller)"
   "ready" = "Return whether Helm server side component (Tiller) is ready"
   "setup" = "Install or update Helm server side component (Tiller)"
   "uninstall" = "Uninstall Helm server side component (Tiller)"
   "upgrade" = "Upgrade Helm server side component (Tiller)"
   "wait" = "Wait for Helm server side component (Tiller) to be ready"
}