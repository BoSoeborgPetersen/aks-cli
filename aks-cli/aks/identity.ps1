SubMenu @{
    "authorize" = "Authorize AKS cluster Managed Identity to access global resources (cluster Resource Group & Azure Container Registry)"
    "check" = "Check AKS cluster Managed Identity"
    "get" = "Get AKS cluster Managed Identity ID"
    "unauthorize" = "Unauthorize AKS cluster Managed Identity from accessing global resources (cluster Resource Group & Azure Container Registry)"
}