SubMenu @{
    "check" = "Check Azure Container Registry"
    "clean" = "Delete pre-release images that are older than 1 month"
    "create" = "Create Azure Container Registry"
    "migrate" = "Move images in repository in Azure Container Registry to another repository possible in another registry"
}